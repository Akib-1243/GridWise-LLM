import{BatteryCharging,ChartNoAxesCombined,Zap}from'lucide-react';import{n}from'../../utils/formatters';export function EnergyKpiCards({grid,peak}:{grid:number;peak:number}){return <div className="grid gap-4 sm:grid-cols-3">{[[Zap,'Grid consumption',`${n(grid)} kWh`,'cyan'],[ChartNoAxesCombined,'Peak grid draw',`${n(peak)} kWh`,'amber'],[BatteryCharging,'Energy efficiency','94.8%','emerald']].map(([Icon,label,value,color])=>{const I=Icon as typeof Zap;return <div className="panel p-5" key={label as string}><div className={`mb-4 grid h-10 w-10 place-items-center rounded-xl bg-${color}-100 text-${color}-700`}><I size={20}/></div><p className="label">{label as string}</p><p className="mt-1 text-2xl font-semibold tracking-tight">{value as string}</p></div>})}</div>}
