import{Line,LineChart,ResponsiveContainer,Tooltip,XAxis,YAxis}from'recharts';import type{Row}from'../../types';import{h}from'../../utils/formatters';export function BatteryChart({rows}:{rows:Row[]}){return <div className="panel h-80 p-5"><p className="font-semibold">Battery state timeline</p><p className="muted text-sm">State of charge after each hour</p><ResponsiveContainer width="100%" height="86%"><LineChart data={rows}><XAxis dataKey="hour" tickFormatter={h}/><YAxis/><Tooltip/><Line dataKey="battery_energy_after_kwh" stroke="#6ef2c5" strokeWidth={3} dot={false}/></LineChart></ResponsiveContainer></div>}
