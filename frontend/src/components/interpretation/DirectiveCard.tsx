import{Brain,CheckCircle2,MinusCircle}from'lucide-react';import type{Directive}from'../../types';import{title}from'../../utils/formatters';export function DirectiveCard({note,directive}:{note:string;directive:Directive}){const hours=directive.structured_adjustment?.hours.map(x=>`${String(x).padStart(2,'0')}:00`).join(' · ');return <div className="panel p-4"><div className="flex gap-3"><div className={`mt-1 ${directive.applies?'text-emerald-500':'text-slate-400'}`}>{directive.applies?<CheckCircle2/>:<MinusCircle/>}</div><div className="min-w-0"><p className="text-sm font-medium">“{note}”</p><div className="mt-3 flex flex-wrap gap-2"><span className="rounded-md bg-ink px-2 py-1 text-[10px] font-bold uppercase tracking-wide text-mint dark:bg-mint dark:text-ink">{title(directive.directive_type)}</span>{hours&&<span className="rounded-md bg-slate-100 px-2 py-1 text-xs dark:bg-white/10">{hours}</span>}</div><p className="mt-2 text-xs muted"><Brain className="mr-1 inline h-3 w-3"/>{directive.explanation}</p></div></div></div>}
