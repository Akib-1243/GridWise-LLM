import type{Hour}from'../../types';export function HoursTable({rows,onChange}:{rows:Hour[];onChange:(r:Hour[])=>void}){const set=(i:number,k:keyof Hour,v:number)=>{const next=[...rows];next[i]={...next[i],[k]:v};onChange(next)};return <div className="overflow-x-auto"><table className="w-full min-w-[620px] text-sm"><thead className="text-left muted"><tr><th>Hour</th><th>Demand kWh</th><th>Solar kWh</th><th>Tariff ৳/kWh</th></tr></thead><tbody>{rows.map((r,i)=><tr className="border-t border-slate-100 dark:border-white/10" key={r.hour}><td className="py-2 font-medium">{String(r.hour).padStart(2,'0')}:00</td>{(['demand_kwh','solar_kwh','tariff_bdt_per_kwh'] as const).map(k=><td key={k} className="py-2 pr-2"><input className="input py-1.5" type="number" min="0" value={r[k]} onChange={e=>set(i,k,Number(e.target.value))}/></td>)}</tr>)}</tbody></table></div>}
