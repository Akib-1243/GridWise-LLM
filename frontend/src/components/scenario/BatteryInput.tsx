import type{Battery}from'../../types';export function BatteryInput({value,onChange}:{value:Battery;onChange:(b:Battery)=>void}){const fields=[['capacity_kwh','Capacity'],['initial_energy_kwh','Initial charge'],['minimum_energy_kwh','Minimum reserve'],['max_charge_kwh_per_hour','Max charge/hour'],['max_discharge_kwh_per_hour','Max discharge/hour']] as const;return <div className="grid gap-3 sm:grid-cols-2">{fields.map(([key,label])=><label key={key}><span className="label">{label} · kWh</span><input className="input mt-1" type="number" min="0" value={value[key]} onChange={e=>onChange({...value,[key]:Number(e.target.value)})}/></label>)}</div>}
