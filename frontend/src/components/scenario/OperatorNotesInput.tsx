export function OperatorNotesInput({notes,onChange}:{notes:string[];onChange:(n:string[])=>void}){const change=(i:number,v:string)=>onChange(notes.map((n,x)=>x===i?v:n));return <div className="space-y-2">{notes.map((note,i)=><textarea key={i} className="input min-h-20 resize-y" value={note} onChange={e=>change(i,e.target.value)} placeholder="Describe a real operating condition…"/>)}{notes.length<3&&<button type="button" className="text-sm font-semibold text-cyan" onClick={()=>onChange([...notes,''])}>+ Add another note</button>}</div>}
